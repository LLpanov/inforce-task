import { FC } from 'react';
import { Center } from '@chakra-ui/react';

const Admin: FC = () => {
	return (
		<>
			<Center height={'100vh'}>
				doesn't work...
			</Center>
		</>
	);
};

export { Admin };